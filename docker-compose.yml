version: '3.8'

services:
  spell-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spell-bot
    restart: unless-stopped
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
      - DEBUG_MODE=${DEBUG_MODE:-false}
      - SQLITE_PATH=${SQLITE_PATH:-/app/storage/storage.db}
    volumes:
      - ./logs:/app/logs
      - ./storage:/app/storage  # Сохраняем БД между перезапусками
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Monitoring сервисы закомментированы, так как нет конфигурационных файлов
  # Раскомментирую после настройки и создания, если понадобятся ./monitoring/prometheus.yml и настройки Grafana

  # prometheus:
  #   image: prom/prometheus:latest
  #   container_name: prometheus
  #   ports:
  #     - "9090:9090"
  #   volumes:
  #     - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
  #     - prometheus_data:/prometheus
  #   command:
  #     - '--config.file=/etc/prometheus/prometheus.yml'
  #     - '--storage.tsdb.path=/prometheus'
  #   restart: unless-stopped

  # grafana:
  #   image: grafana/grafana:latest
  #   container_name: grafana
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD:-admin}
  #   volumes:
  #     - ./monitoring/grafana/provisioning:/etc/grafana/provisioning
  #     - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards
  #     - grafana_data:/var/lib/grafana
  #   restart: unless-stopped
  #   depends_on:
  #     - prometheus

# volumes:
#   prometheus_data:
#   grafana_data: